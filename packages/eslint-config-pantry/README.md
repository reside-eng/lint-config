# @side/eslint-config-pantry

A lint configuration for the consumers of Pantry.

## Installation

`yarn add -D @side/eslint-config-pantry`

## Usage

Extend this configuration in your project's `.eslintrc.js`.

**.eslintrc.js**

```js
module.exports = {
  extends: ['@side/pantry'],
};
```
