{
  "version": "independent",
  "npmClient": "yarn",
  "packages": ["packages/*"],
  "useWorkspaces": true,
  "command": {
    "publish": {
      "ignoreChanges": ["*.md", "docs"],
      "message": "chore(release): publish %s"
    }
  }
}
